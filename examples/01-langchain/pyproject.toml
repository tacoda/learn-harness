[project]
name = "learn-harness-01-langchain"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.0",
    "langchain-openai",
    "langchain-text-splitters",
    "langgraph>=1.0",
    "pydantic",
    "python-dotenv",
]

[tool.uv]
package = false
