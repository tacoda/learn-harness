[project]
name = "learn-harness-05-expert"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.0",
    "langchain-openai",
    "langgraph>=1.0",
    "python-dotenv",
]

[tool.uv]
package = false
